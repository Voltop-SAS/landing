version: '3.8'
services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
      target: runner
    volumes:
      - .:/workspace
      - /workspace/node_modules
      - /workspace/.next
    command: npm run dev
    ports:
      - "3000:3000"
    environment:
      NODE_ENV: development
